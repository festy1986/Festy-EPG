name: Festy TVGuide

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Confirm workflow appears
        run: echo "Festy TVGuide workflow is working"
