name: Festy OTA Guides

on:
  workflow_dispatch:
  schedule:
    - cron: "30 */12 * * *"

permissions:
  contents: write

jobs:
  build-ota-guides:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build OTA raw and curated guides
        run: |
          mkdir -p guides scripts
          node scripts/grab-tvguide-ota.js
        env:
          TVGUIDE_PROVIDER_ID: 9100000664
          DAYS: 14
          RAW_OUTPUT: guides/OTALocalsguide.raw.xml
          CURATED_OUTPUT: guides/OTALocalsguide.xml

      - name: Commit OTA guides
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"

          git add guides/OTALocalsguide.raw.xml guides/OTALocalsguide.xml
          git commit -m "Update OTA raw and curated guides" || echo "No changes to commit"
          git push
